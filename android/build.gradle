buildscript {
    repositories {
        google()  // ← IMPORTANTE
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.11.2'
    }
}

allprojects {
    repositories {
        google()  // ← IMPORTANTE
        mavenCentral()
    }
}
